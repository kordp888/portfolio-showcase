# 검증 기록

기준일: 2026-09-13. 과거 검사는 각 항목에 실행일을 따로 표시합니다.

## Career Coach Production 확인 (2026-09-13)

[실제 웹앱](https://career-insight-coach.vercel.app)의 Git 자동 배포를 확인했습니다.
배포된 버전 `b4e591f7f0a9`는 당시 main 커밋과 일치합니다. 수동 배포는 사용하지 않았습니다.
홈, `/coach`, `/api/status`, `/api/version`은 모두 HTTP 200이었고 AI 연결 상태도 정상이었습니다.

산업·기업·직무 분석, 경험 보완, 인사이트, 문서 생성의 여섯 작업이 실제 AI 응답을 반환했습니다.
빈 입력은 HTTP 400, 제한된 요청은 HTTP 429로 처리되는 것을 확인했습니다.

개인정보 없는 합성 경험 두 개를 브라우저에 직접 입력했습니다. 경험 보완과 역량 연결,
인사이트 선택, 자소서 설계안과 초안, 이력서 문장, 포트폴리오 Case Study 생성까지 완료했습니다.
문서 수정과 복사, 새로고침 후 저장 유지, JSON 내보내기·삭제·불러오기,
잘못된 JSON 거부와 예시 공간 분리도 확인했습니다.

AI 요청을 브라우저에서 의도적으로 실패시킨 검사에서는 여섯 화면 모두 오류를 표시했습니다.
예시 데이터로 자동 대체하지 않았습니다. 같은 화면의 HTTP 429 안내도 검사했습니다.
375·390·430·768·1024·1440px 브라우저 화면에서 가로 넘침이 없었고,
정상 사용 흐름에서 브라우저 콘솔 오류가 없었습니다. 물리 기기 검사는 아닙니다.

이 기록은 해당 시점의 기능 동작 검사입니다. AI 분석의 사실 정확도나 합격률,
실제 사용자 성과, 상시 가용성을 검증한 것은 아닙니다.

## 직접 확인한 기술 결과

운영 도구의 로컬 테스트를 macOS에서 실행해 113개 통과, 실패 0개를 확인했습니다.
상태 조회, 문맥 선택, 제한 작업 위임, 중복 실행 방지, 로컬 연결 관련 검사를 포함합니다.
이 수치는 개별 제품 전체의 테스트 수가 아니며 상용 운영의 무오류를 보증하지 않습니다.

프로젝트 참고문서를 복사 등록한 뒤 원본과 사본이 중복 선택되는 결함을 발견했습니다.
관리 사본을 사용하도록 수정하고, 임시 원본을 제거한 경우에도 한 번만 로드되는지 검사했습니다.
세 프로젝트의 실제 문맥 선택에서도 각 문서가 한 번씩 포함되는 것을 확인했습니다.

## 화면이 보여주는 범위

WESOP은 2026-09-09 보관된 QA 기록의 단위·빌드 검사 58개와 공개 HTTPS 브라우저 검사 278개 통과를 요약했습니다. 이 페이지를 만들며 해당 제품 검사를 다시 실행한 것은 아닙니다. 실제 참여자 0명, 사용자 가설 NOT_RUN, 물리 기기 검사는 미실행입니다. 공개 화면의 매장·근무·후보는 합성 데이터입니다. 실제 서비스 MVP나 기업의 공식 출시 제품으로 소개하지 않습니다.

ONDA는 2026-09-10 보관된 데모 주행 화면입니다. 좌표와 단속 정보는 모의 데이터입니다.
실차 주행이나 출시 승인을 뜻하지 않습니다.

다시ON5060 이미지는 2026-09-13 [Production 홈](https://dasion-app.vercel.app)에서 새로 캡처했습니다.
홈의 HTTP 200과 정상 화면, 브라우저 콘솔 오류 없음을 확인했습니다.
고령 사용자 사용성 결과와 실기기 음성 검사는 이번 범위에 포함하지 않았습니다.

AI Career Insight Coach 이미지는 같은 날 Production에서 실제 AI로 생성한 인사이트 화면입니다.
테스트용 합성 경험만 사용했습니다. 계정·연락처·실제 위치·원본 업무 기록은 포함하지 않았습니다.

## 공개하지 않는 자료

운영 코드와 알고리즘, 테스트 구현, 시스템 프롬프트, 내부 설계 상세,
기업 원자료, 인증 정보, 개인 데이터, 원본 로그는 비공개 저장소에서 보관합니다.
이 공개 저장소에는 이전 구현 저장소의 커밋 이력을 가져오지 않았습니다.
개인 소개 페이지를 표시하는 정적 HTML/CSS는 공개됩니다. 제품의 구현 코드와는 분리되어 있습니다.
